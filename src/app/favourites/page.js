import Loader from '../../components/common/Loader'

const Page = async ({ params }) => {
    return (
        <>
            <Loader />
            <div></div>
        </>
    )
}

export default Page
